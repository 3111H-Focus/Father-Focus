<?xml version="1.0" encoding="utf-8"?>
<!--Define the layout that shows when user pull the list-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
  type="@style/pull_headerContainer">
  <RelativeLayout 
    android:id="@+id/header" style="@style/pull_header">
    <ImageView android:id="@+id/image" style="@style/pull_arrow"/>
    <TextView android:id="@+id/text" style="@style/pull_text"/>
  </RelativeLayout>
</LinearLayout>
